attribute:
		HTML预定义属性
		HTML自定义属性
property:
		js原生对象的直接属性
		
每一个预定义对象的attri都会有一个property与之对应

布尔值属性: 
非布尔值属性:

非布尔值属性: property和attribute的同步问题
	无论什么情况，property和attribute都会同步
	
布尔值属性: property和attribute的同步问题
	1.改变property时不会同步attribute
	2.在没有改变property时，attribute会同步property
		一旦改变property时，attribute不会同步property

	5.css声明的优先级
		选择器的特殊性
			选择器的特殊性由选择器本身的组件确定，特殊性值表述为4个部分，如    0,0,0,0
			一个选择器的具体特殊性如下确定：
			       1.对于选择器中给定的ID属性值，加 0,1,0,0
			       2.对于选择器中给定的各个类属性，属性选择，或伪类，加 0,0,1,0
			       3.对于选择器中的给定的各个元素和伪元素，加0,0,0,1
			       4.通配符选择器的特殊性为0,0,0,0
			       5.结合符对选择器特殊性没有一点贡献
			       6.内联声明的特殊性都是1,0,0,0
			       7.继承没有特殊性
 